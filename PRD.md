# PRD — "죽음의 멀리갈리" 초대장 (현재 버전 v2)

> 이 문서 하나로 현재 배포 버전의 초대장을 그대로 재현할 수 있도록 작성함.
> 실제 구현 파일: `docs/index.html`(단일 파일: HTML+CSS+JS), `docs/config.js`(내용/키), 이미지 에셋 폴더들.

---

## 1. 개요

행사 초대장 웹페이지. 접속하면 **한강공원 잔디밭 위에서 할리갈리(멀리갈리) 게임 → 종 치기 → 얼굴 폭죽** 오프닝 애니메이션이 자동 재생되고, 이어서 **행사 정보 + 방명록**이 나타난다.

- **형태**: 정적 단일 페이지 (모바일 우선, 세로 화면 기준). 사운드 없음.
- **프론트**: 순수 HTML / CSS / Vanilla JS + Canvas 2D(폭죽).
- **방명록 백엔드**: Supabase(무료). 미설정 시 localStorage 데모 모드로 자동 폴백.
- **호스팅**: 정적 호스팅(GitHub Pages / Vercel / Netlify 등).
- **외부 의존성**: Supabase JS v2 (`https://cdn.jsdelivr.net/npm/@supabase/supabase-js@2`) — 이거 하나만 CDN.

---

## 2. 행사 정보 (콘텐츠) — `config.js`

`window.INVITE_CONFIG` 객체로 관리. 실제로 렌더에 사용되는 필드는 아래와 같다.

| 필드 | 값(현재) | 렌더 위치 |
|------|----------|-----------|
| `event.host` | `MK와 아이들` | 히어로 주최자 줄 |
| `event.dateText` | `""`(빈 값) → **"미정 · 추후 공지"** 표시(금색) | 정보카드 "일시" |
| `event.place` | `뚝섬 한강공원` | 정보카드 "장소" |
| `event.mapNaver` | 네이버 지도 검색 URL | "네이버 지도" 버튼 |
| `event.mapGoogle` | 구글 지도 검색 URL | "구글 지도" 버튼 |
| `event.dress` | `작업복 & 보호대` | 정보카드 "드레스코드 & 준비물" |
| `supabase.url` / `.anonKey` / `.table` | 빈 값 → 데모 모드 / `guestbook` | 방명록 |

- 제목("죽음의 멀리갈리")과 부제("멀리갈리로 갈 때까지 가보기")는 **마크업에 하드코딩**(히어로 `h1`, `.sub`). `<title>`, OG 태그도 하드코딩.
- 날짜가 정해지면 `dateText`에 문자열만 넣으면 됨(예: `"2026년 9월 20일 (토) 오후 6시"`).

---

## 3. 에셋 (이미지) 사양 & 생성 방법

모두 배경 제거(누끼)된 투명 PNG. `docs/` 하위에 위치.

### 3.1 인물 캐릭터 — `docs/chars/{id}.png` (10개)
- **정체**: 10명 전신 캐리커처(큰 머리 스타일). 흰옷 계열.
- **원본**: `assets/src/char_ref.png` (5열×2행 그리드, 1536×1024). 각 셀 1명.
- **생성**: 각 셀을 잘라 `rembg`(u2net)로 흰 배경 제거 → 알파 bbox로 트림 → **세로 420px**로 정규화.
- **id 목록(고정 순서)**:
  `01_man1_left, 02_man2_glass, 03_man3_center, 04_woman_offsh, 05_woman_left, 06_woman_pony, 07_woman_bang, 08_woman_blue, 09_woman_smile, 10_man4_right`
- 그리드→id 매핑: 위줄 = 01,02,03,04,10 / 아랫줄 = 05,06,07,08,09.

### 3.2 폭죽용 얼굴 — `docs/faces_char/{id}.png` (10개)
- **정체**: 위 캐릭터에서 **얼굴(머리)만** 원형으로 크롭.
- **생성**: 각 `chars/*.png`의 상단 머리 영역(상위 36% 밴드)의 알파 무게중심을 얼굴 중심으로 잡고, 반경 ≈ 피규어높이×0.205로 정사각 크롭 → **256×256** → 원형 마스크(가우시안 페더).

### 3.3 할리갈리 카드 — `docs/cards/{banana,lime,plum,straw}.png` (4개)
- **정체**: 흰 카드 위 과일 그림. 바나나×1, 라임×3, 자두×4, 딸기×5.
- **원본**: `assets/src/cards.png`(카드 사진). 하단 개별 카드 4장 영역을 사용.
- **생성**: 각 카드 영역 크롭 → "흰 카드" 픽셀(밝고 저채도)로 이진 마스크 → 최대 연결요소 + 구멍채우기(scipy `binary_fill_holes`)로 **카드 전체(흰 배경+과일)** 를 살림(과일만 남기면 안 됨) → 알파 페더 → **세로 300px**.

### 3.4 종 — `docs/bell.png`
- **정체**: 실제 할리갈리 탁상벨(크롬 돔 + 검은 버튼 + 검은 받침).
- **원본**: `assets/src/bell.jpg`. `rembg`로 배경 제거 → 트림.

> 소스 원본(`assets/src/char_ref.png`, `cards.png`, `bell.jpg`)만 있으면 위 파이프라인으로 전 에셋 재생성 가능. (rembg, Pillow, numpy, scipy 필요)

---

## 4. 화면 구조

페이지는 두 레이어.
1. **`#intro`** — 풀스크린 고정(`position:fixed; z-index:50`). 오프닝 애니메이션. 끝나면 `.done`으로 페이드아웃(0.8s) 후 `display:none`.
2. **`#content`** — 초대장 본문. 초기 `opacity:0`, 공개 시 `.show`로 페이드인(1s). 최대폭 560px 중앙.

---

## 5. 오프닝 애니메이션 (핵심)

### 5.1 무대(배경) 구성 — `#intro` 내부 레이어
- **하늘**: 저녁 노을 세로 그라데이션(남색→보라→분홍→살구).
- **강 건너 도시 실루엣**(`.skyline`) + **불빛 보케**(`.bokeh`): 작은 점광 46개, 위치·크기·딜레이 랜덤, 깜빡임(`tw` 3s).
- **한강 물**(`.river`): 얇은 띠 + 물결 반복 패턴.
- **잔디밭**(`.grass`): 초록 그라데이션 + 잔디결 텍스처.
- **힌트 텍스트**(`.hint`): "🃏 뚝섬 한강공원, 죽음의 멀리갈리 한 판이 진행 중…" (상단, 페이드인).
- **무대 컨테이너**(`.scene`): 화면 중앙 정렬(`width:min(92vw,520px)`), 이 안에 돗자리/캐릭터/카드/종 배치(좌표는 scene 대비 %).

### 5.2 무대 요소
- **돗자리(`.mat`)**: 정사각(가로 = scene 48%, 세로비 0.9), 살짝 회전(-2deg), 빨간 깅엄 체크, 흰 안쪽 테두리. scene 중앙(50%,56%).
- **종(`.bell`)**: `bell.png`, 폭 86px, 돗자리 중앙(50%,55%).
- **손(`.hand`)**: 🖐️ 이모지(40px), 평소 숨김.
- **캐릭터(`.char`) 10명**: 돗자리 둘레에 배치. 각 캐릭터는 이미지 높이 120px(모바일 104px), 평소 **idle 바운스**(위아래 4px + ±1.5deg 회전, 2.8s, 시작 딜레이 랜덤).
  - 좌표(scene % 기준): 위 `(30,26)(50,22)(70,26)`, 왼 `(16,46)(18,67)`, 오른 `(84,46)(82,67)`, 아래 `(32,81)(50,85)(68,81)`. 배열 순서 = 위 id 순서.
- **카드(`.card`) 4장**: 돗자리 위. 3D 플립 구조.
  - 종류/좌표: `lime(40,48)`, `straw(60,49)`, `plum(44,64)`, `banana(59,63)`.
  - 각 카드 랜덤 회전(-18°~+18°).
  - **앞면(`.front`)**: 과일 카드 이미지(높이 48px, 모바일 42px). **뒷면(`.back`)**: 빨간 그라데이션 + 흰 테두리 + 점선 원 엠블럼.
  - 플립: `.flip`이 `rotateY(180deg)`(뒷면) → `.card.revealed`면 `rotateY(0)`(앞면). transition 0.6s.

### 5.3 타임라인 (자동 재생; t=0 = 시퀀스 시작)

시퀀스 시작(`start()`)은 폭죽 얼굴 프리로드 완료 시 또는 **최대 1.5s 폴백** 중 먼저.

| 시각 | 동작 |
|------|------|
| t=0 | 무대 표시. 카드는 **뒷면**. 캐릭터 idle 바운스. |
| **t=1.0s+** | 카드가 **한 장씩 순차 플립**. 시작 1.0s, 이후 **0.3s 간격**. 순서는 **랜덤**이되 **딸기는 항상 마지막**. (라임/자두/바나나 셋을 셔플 후 딸기를 뒤에 붙임) |
| **t=2.0s** | **랜덤으로 선택된 1명**(접속/다시보기마다 재추첨)이 종을 향해 대시(transform 이동 + 살짝 확대). |
| t=2.56s | 손(🖐️) 슬램 애니메이션 시작 + 러너 idle 정지(`.run`). |
| **t=2.7s** | 종 흔들림(`bellShake`) + 화면 플래시 + **폭죽 시작**(3초). |
| **t=5.95s** | 오프닝 페이드아웃 + **본문 공개**. |

- **건너뛰기 버튼**(`.skip`, 우하단): 즉시 본문 공개.
- **다시 보기 버튼**(본문 푸터 `.replay`): 전체 리셋 후 재생(러너·카드순서 재랜덤).

### 5.4 폭죽 사양 (Canvas 2D, `#fx`)
- **캔버스**: 뷰포트 전체(`width/height:100%` 명시 필수 — 안 그러면 좌표가 어긋나 한쪽에서 터짐). 내부 해상도 = CSS크기 × DPR(최대 2). 그리기는 CSS px 좌표.
- **발사 위치**: **종의 화면 중심**(`getBoundingClientRect` 기반) = 화면 정중앙.
- **지속**: 첫 버스트(파티클 14, power 7.5) 후, **0.3s마다 버스트를 3.0s 동안** 반복. 반복 버스트는 중심에서 ±45/±40px 지터, 파티클 8~12, power 6~8.5.
- **파티클(얼굴)**: `faces_char` 이미지 사용. **크기 54~98px**(잘 보이게 크게). 방사형 초기속도(약간 위로 편향), 중력(vy+=0.08), 약한 감속(vx×0.99), 회전 드리프트, 수명 감쇠(0.006~0.012/frame ≈ 1.4~2.8s). 각 얼굴 뒤에 컬러 **글로우 링**(팔레트에서 색 순환).
- **스파크**: 버스트당 파티클수×6개의 작은 점(중력 0.05, 빠른 감쇠), 컬러 팔레트 랜덤.
- **컬러 팔레트**: `#ffd76b, #ff5d8f, #5ad1ff, #9b7bff, #7CFF9E, #ff8a3d, #fff`.
- 접근성: `prefers-reduced-motion` 시 애니메이션 사실상 즉시 처리.

---

## 6. 본문(`#content`)

1. **히어로**: eyebrow "INVITATION" / `h1` "죽음의 / 멀리갈리"(금색 그라데이션) / 부제 "멀리갈리로 갈 때까지 가보기" / 주최자(`event.host`).
   - ⚠️ **사람 얼굴 링 없음**(제거됨).
2. **정보 카드**(3행): 🗓️ 일시(값 없으면 "미정 · 추후 공지") · 📍 장소(+ 네이버/구글 지도 버튼) · 🧥 드레스코드 & 준비물.
   - ⚠️ "한 줄 소개" 행 없음(제거됨).
3. **방명록 섹션**(아래 7).
4. **푸터**: "죽음의 멀리갈리 · 뚝섬 한강공원" + 🔔 오프닝 다시 보기 버튼.

---

## 7. 방명록

- 입력: **닉네임**(최대 20자) + **메시지**(최대 300자). 등록 시 목록에 **pop 애니메이션**으로 추가.
- 목록: 최신순, 최대 200개. 시각 표기 `M.D HH:MM`. 모든 출력 **HTML 이스케이프**.
- 빈 상태: "아직 방명록이 없어요. 첫 인사를 남겨보세요! 👋".

### 7.1 Supabase 연결 시
- `supabase.url/anonKey` 설정되면 자동으로 Supabase 사용. 테이블 `guestbook`.
- **스키마**:
  | 컬럼 | 타입 | 비고 |
  |------|------|------|
  | id | uuid PK | `default gen_random_uuid()` |
  | nickname | text | 1~20자 |
  | message | text | 1~300자 |
  | created_at | timestamptz | `default now()` |
- **RLS**: 읽기 전체 허용(`select using(true)`), 삽입 전체 허용(`insert with check(true)`). 수정/삭제 정책 없음(불가).
- 조회: `select * order by created_at desc limit 200`. 삽입: `{nickname, message}`.

### 7.2 미설정 시(데모 모드)
- `localStorage`(키 `mgb_entries`)에 저장, 최대 200개. 하단에 "데모 모드 · 지금은 이 브라우저에만 저장됩니다" 안내.

---

## 8. 비주얼 디자인 토큰

- **폰트**: `Apple SD Gothic Neo, Pretendard, Noto Sans KR, system-ui …`.
- **본문 배경**: 다크 네이비 라디얼 그라데이션. **오프닝 배경**: 저녁 노을.
- **주요 색**: 금색 `--gold #ffd76b`/`--gold2 #ffb03a`, 잉크 `#f6f4ef`, 뮤트 `#a9b4d0`, 라인 `rgba(255,255,255,.14)`, 포인트 핑크 `#ff5d8f`.
- **파비콘**: 🔔 이모지(SVG data URI).

---

## 9. 파일 구조 (배포)

```
docs/                    ← 이 폴더가 사이트 루트 (GitHub Pages=/docs)
├─ index.html            (전부 인라인: 마크업+스타일+스크립트)
├─ config.js             (행사 정보 + Supabase 키)
├─ bell.png
├─ cards/  banana.png lime.png plum.png straw.png
├─ chars/  01_…png … 10_…png            (전신 캐릭터 10)
└─ faces_char/ 01_…png … 10_…png        (폭죽용 얼굴 10)
assets/src/              ← 재생성용 원본(비배포): char_ref.png, cards.png, bell.jpg
```

---

## 10. 동작 요구사항 체크리스트 (수용 기준)

- [ ] 접속 시 오프닝 자동 재생(무음). 저녁 노을 한강공원 + 잔디 돗자리 + 10명 캐릭터 + 중앙 종.
- [ ] 카드가 뒷면으로 시작 → 1.0s부터 0.3s 간격 **순차 플립**, **랜덤 순서 + 딸기 마지막**.
- [ ] 2.0s에 **랜덤 인물 1명**이 종으로 대시(새로고침마다 달라짐).
- [ ] 손이 종을 치면 **화면 정중앙**에서 **얼굴(캐릭터) 폭죽 3초**. 넓은 데스크톱에서도 중앙 유지.
- [ ] 폭죽 종료 후 본문 공개(제목/부제/주최자, 일시=미정, 장소+지도버튼, 드레스코드).
- [ ] 본문에 실제 인물 얼굴/한 줄 소개 없음.
- [ ] 방명록 작성/조회 동작(Supabase 또는 localStorage 폴백), XSS 이스케이프.
- [ ] 건너뛰기/다시 보기 버튼 정상.
- [ ] 모바일·데스크톱 반응형, `prefers-reduced-motion` 대응.

---

## 11. 배포 메모

- 정적 파일만 서빙하면 됨. 로컬 확인: `cd web && python3 -m http.server 8000`.
- `file://` 직접 열기 금지(상대경로 이미지 로드 실패) — 반드시 서버로.
- GitHub Pages(무료)는 **public 저장소** 또는 Pro 필요. private 유지하며 무료 배포하려면 Vercel/Netlify/Cloudflare Pages 권장.
- repo에 시크릿 없음(Supabase anon 키는 공개용, RLS로 보호). 실제 인물 사진은 포함하지 않음.
